name: "pwd"
cases:
  - name: prints_working_directory
    stdin: |
      pwd
    expect_exit: 0
    expect_stdout_contains: "/"
    expect_stderr_empty: true

  - name: reflects_cd
    stdin: |
      cd /tmp && pwd
    expect_exit: 0
    expect_stdout_contains: "/tmp"
    expect_stderr_empty: true
