name: "local"
cases:
  - name: scopes_variable_to_function
    stdin: |
      f() { local v=inner; echo $v; }
      f
      echo ${v:-outer}
    expect_exit: 0
    expect_stdout: |
      inner
      outer
    expect_stderr_empty: true
